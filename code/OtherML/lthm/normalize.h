#ifndef NORMALIZE_HH
#define NORMALIZE_HH

#include <vector>

using namespace std;

// Divides each entry of the vector v by the constant norm.
void Normalize(double norm, vector<double> *v);

#endif

